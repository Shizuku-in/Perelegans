<mah:MetroWindow x:Class="Perelegans.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:models="clr-namespace:Perelegans.Models"
        Title="Settings"
        TitleCharacterCasing="Normal"
        Height="700" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowTitleBar="True"
        TitleBarHeight="28"
        BorderThickness="1"
        WindowTitleBrush="#FFC0CB"
        NonActiveWindowTitleBrush="#FFC0CB"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        Background="{DynamicResource Perelegans.WindowBackground}"
        Foreground="{DynamicResource Perelegans.ForegroundBrush}"
        xmlns:i18n="clr-namespace:Perelegans.i18n">

    <mah:MetroWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <ObjectDataProvider x:Key="ThemeModes" MethodName="GetValues" ObjectType="{x:Type models:ThemeMode}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:ThemeMode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </mah:MetroWindow.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled"
                  Padding="16,12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="{i18n:Translate Settings_Appearance}" Margin="0,0,0,8"
                      Padding="8,6" FontSize="12"
                      BorderBrush="{DynamicResource Perelegans.BorderBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="{i18n:Translate Settings_ThemeMode}" VerticalAlignment="Center" FontSize="12"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Height="26" FontSize="12"
                              ItemsSource="{Binding Source={StaticResource ThemeModes}}"
                              SelectedItem="{Binding SelectedTheme, Mode=TwoWay}"/>

                    <TextBlock Grid.Row="2" Text="{i18n:Translate Settings_Language}" VerticalAlignment="Center" FontSize="12"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Height="26" FontSize="12"
                              ItemsSource="{Binding LanguageOptions}"
                              SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="{i18n:Translate Settings_Monitor}" Margin="0,0,0,8"
                      Padding="8,6" FontSize="12"
                      BorderBrush="{DynamicResource Perelegans.BorderBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="{i18n:Translate Settings_Enabled}" VerticalAlignment="Center" FontSize="12"/>
                    <CheckBox Grid.Row="0" Grid.Column="1"
                              IsChecked="{Binding MonitorEnabled, Mode=TwoWay}"
                              VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="2" Text="{i18n:Translate Settings_Interval}" VerticalAlignment="Center" FontSize="12"/>
                    <mah:NumericUpDown Grid.Row="2" Grid.Column="1"
                                       Value="{Binding MonitorIntervalSeconds, Mode=TwoWay}"
                                       Minimum="1" Maximum="60" Interval="1"
                                       Width="70"
                                       Height="26" FontSize="12"
                                       HorizontalAlignment="Left"
                                       NumericInputMode="Numbers"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="2" Header="{i18n:Translate Settings_Network}" Margin="0,0,0,8"
                      Padding="8,6" FontSize="12"
                      BorderBrush="{DynamicResource Perelegans.BorderBrush}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{i18n:Translate Settings_Proxy}" VerticalAlignment="Center" FontSize="12"/>
                    <TextBox Grid.Column="1" Height="26" FontSize="12"
                             VerticalContentAlignment="Center"
                             Text="{Binding ProxyAddress, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             mah:TextBoxHelper.Watermark="http://127.0.0.1:7890"/>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="3" Header="{i18n:Translate Settings_Ai}" Margin="0,0,0,8"
                      Padding="8,6" FontSize="12"
                      BorderBrush="{DynamicResource Perelegans.BorderBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="8"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="{i18n:Translate Settings_AiProvider}" VerticalAlignment="Center" FontSize="12"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" Height="26" FontSize="12"
                              ItemsSource="{Binding AiProviderOptions}"
                              DisplayMemberPath="Label"
                              SelectedValuePath="Value"
                              SelectedValue="{Binding SelectedAiProvider, Mode=TwoWay}"/>

                    <Border Grid.Row="2" Grid.Column="1"
                            Padding="8,6"
                            Background="{DynamicResource Perelegans.PanelBackground}"
                            BorderBrush="{DynamicResource Perelegans.BorderBrush}"
                            BorderThickness="1">
                        <TextBlock
                               Text="{Binding AiProviderHint}"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource Perelegans.SubtleForegroundBrush}"
                               FontSize="11"/>
                    </Border>

                    <TextBlock Grid.Row="4" Text="{i18n:Translate Settings_AiBaseUrl}" VerticalAlignment="Center" FontSize="12"/>
                    <TextBox Grid.Row="4" Grid.Column="1" Height="26" FontSize="12"
                             VerticalContentAlignment="Center"
                             Text="{Binding AiApiBaseUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             mah:TextBoxHelper.Watermark="{Binding AiBaseUrlPlaceholder}"/>

                    <TextBlock Grid.Row="6" Text="{i18n:Translate Settings_AiApiKey}" VerticalAlignment="Center" FontSize="12"/>
                    <TextBox Grid.Row="6" Grid.Column="1" Height="26" FontSize="12"
                             VerticalContentAlignment="Center"
                             Text="{Binding AiApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             mah:TextBoxHelper.Watermark="{Binding AiApiKeyPlaceholder}"/>

                    <TextBlock Grid.Row="8" Text="{i18n:Translate Settings_AiModel}" VerticalAlignment="Center" FontSize="12"/>
                    <TextBox Grid.Row="8" Grid.Column="1" Height="26" FontSize="12"
                             VerticalContentAlignment="Center"
                             Text="{Binding AiModel, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             mah:TextBoxHelper.Watermark="{Binding AiModelPlaceholder}"/>

                    <DockPanel Grid.Row="10" Grid.Column="1" LastChildFill="True">
                        <Button DockPanel.Dock="Left"
                                Width="96" Height="28"
                                Margin="0,0,8,0"
                                BorderBrush="{DynamicResource Perelegans.ButtonBorderBrush}"
                                Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                                Content="{i18n:Translate Settings_AiTestButton}"
                                Command="{Binding TestAiCommand}"/>
                        <Border Padding="8,5"
                                Background="{DynamicResource Perelegans.PanelBackground}"
                                BorderBrush="{DynamicResource Perelegans.BorderBrush}"
                                BorderThickness="1"
                                Visibility="{Binding HasAiTestStatus, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{Binding AiTestStatusText}"
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource Perelegans.SubtleForegroundBrush}"
                                       FontSize="11"/>
                        </Border>
                    </DockPanel>
                </Grid>
            </GroupBox>

            <GroupBox Grid.Row="4" Header="{i18n:Translate Settings_System}" Margin="0,0,0,8"
                      Padding="8,6" FontSize="12"
                      BorderBrush="{DynamicResource Perelegans.BorderBrush}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="6"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="{i18n:Translate Settings_LaunchAtStartup}" VerticalAlignment="Center" FontSize="12"/>
                    <CheckBox Grid.Row="0" Grid.Column="1"
                              IsChecked="{Binding LaunchAtStartup, Mode=TwoWay}"
                              VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="2" Text="{i18n:Translate Settings_CloseBehavior}" VerticalAlignment="Center" FontSize="12"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" Height="26" FontSize="12"
                              ItemsSource="{Binding CloseBehaviorOptions}"
                              DisplayMemberPath="Label"
                              SelectedValuePath="Value"
                              SelectedValue="{Binding SelectedCloseBehavior, Mode=TwoWay}"/>
                </Grid>
            </GroupBox>

            <StackPanel Grid.Row="5" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,4,0,0">
                <Button Content="{i18n:Translate Btn_Save}" Width="80" Height="28" FontSize="12"
                        Margin="0,0,8,0"
                        BorderBrush="{DynamicResource Perelegans.ButtonBorderBrush}"
                        Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                        Click="BtnSave_Click"/>
                <Button Content="{i18n:Translate Btn_Cancel}" Width="80" Height="28" FontSize="12"
                        BorderBrush="{DynamicResource Perelegans.ButtonBorderBrush}"
                        Style="{DynamicResource MahApps.Styles.Button.Square}"
                        Click="BtnCancel_Click"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</mah:MetroWindow>
